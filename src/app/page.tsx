import { Typography } from '@mui/material';
import { APP_NAME } from '@/constants/content';

export default function LandingPage() {
  return <Typography>{APP_NAME} - Landing Placeholder</Typography>;
}
